#include <stdio.h>
#include <stdlib.h>
int main( int argc, char * argv[])
{
	int i, dem=0;
	int n= atoi(argv[1]);
	if (argc==2 && n>0)
	{
		for (i=1; i<=n; i++)
		{
			if (n%i==0)
			{
				printf("%d\t",i);
			}
		}
		printf("\n%d = ",n);
		for (i=2; i<=n; i++)

